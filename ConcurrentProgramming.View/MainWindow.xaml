<Window x:Class="ConcurrentProgramming.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:ConcurrentProgramming.ViewModel;assembly=ConcurrentProgramming.ViewModel"
        mc:Ignorable="d"
        Title="Programowanie współbieżne kulki" Height="450" Width="800"
        ResizeMode="CanResize">
    <Window.DataContext>
        <viewModel:MainViewModel />
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="5">
            <Grid.Resources>
                <Style TargetType="{x:Type Label}">
                    <Setter Property="Margin" Value="1" />
                </Style>
                <Style TargetType="{x:Type TextBox}">
                    <Setter Property="Margin" Value="1" />
                </Style>
            </Grid.Resources>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Label VerticalContentAlignment="Center" Grid.Row="0" Grid.Column="0">Ilość kulek</Label>
            <TextBox VerticalContentAlignment="Center" Grid.Row="0" Grid.Column="1" Text="{Binding TargetBallCount}" TextAlignment="Center"/>

            <Button Command="{Binding AddBallsCommand}" Margin="0, 10" Grid.ColumnSpan="2" Grid.Row="1" Padding="5">Ustaw ilość kulek</Button>

            <Label VerticalContentAlignment="Center" Grid.Row="2" Grid.Column="0">Szerokość</Label>
            <TextBox VerticalContentAlignment="Center" Grid.Row="2" Grid.Column="1" Text="{Binding Width}" TextAlignment="Center"/>

            <Label VerticalContentAlignment="Center" Grid.Row="3" Grid.Column="0">Wysokość</Label>
            <TextBox VerticalContentAlignment="Center" Grid.Row="3" Grid.Column="1" Text="{Binding Height}" TextAlignment="Center"/>
        </Grid>

        <Viewbox Grid.Column="1" Stretch="Uniform">
            <Canvas Width="{Binding Width}" Height="{Binding Height}">
                <Rectangle
                    Width="{Binding Width}"
                    Height="{Binding Height}"
                    Fill="LightGray"
                    Stroke="Black" >
                </Rectangle>
                
                <ItemsControl ItemsSource="{Binding Balls}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas>
                            </Canvas>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Ellipse 
                                Height="{Binding Diameter}"
                                Width="{Binding Diameter}"
                                Fill="DarkGray"
                                Stroke="Black"
                            />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Top" Value="{Binding Top}" />
                            <Setter Property="Canvas.Left" Value="{Binding Left}" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>
            </Canvas>
        </Viewbox>
    </Grid>
</Window>
