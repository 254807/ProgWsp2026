<Window x:Class="ConcurrentProgramming.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConcurrentProgramming.View"
        xmlns:data="clr-namespace:ConcurrentProgramming.Data;assembly=ConcurrentProgramming.Data"
        xmlns:numerics="clr-namespace:System.Numerics;assembly=System.Numerics.Vectors"
        xmlns:viewModel="clr-namespace:ConcurrentProgramming.ViewModel;assembly=ConcurrentProgramming.ViewModel"
        xmlns:logic="clr-namespace:ConcurrentProgramming.Logic;assembly=ConcurrentProgramming.Logic"
        mc:Ignorable="d"
        Title="Programowanie współbieżne kulki" Height="450" Width="800"
        ResizeMode="CanResize">
    <Window.DataContext>
        <viewModel:MainViewModel />
    </Window.DataContext>
    
    <ItemsControl ItemsSource="{Binding Balls}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Ellipse 
                    Height="25"
                    Width="25"
                    Fill="DarkGray"
                    Stroke="Black"
                />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="Canvas.Top" Value="{Binding Top}" />
                <Setter Property="Canvas.Left" Value="{Binding Left}" />
            </Style>
        </ItemsControl.ItemContainerStyle>
    </ItemsControl>
</Window>
